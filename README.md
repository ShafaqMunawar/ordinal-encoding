Always define train/test splits before using them.

If your target (purchased) is Yes/No, LabelEncoder is a good choice.

You don’t need to manually type out X_train values — the code will generate them.

